{
  "name": "gamer-backlog-api",
  "version": "0.1.0",
  "private": true,
  "description": "Backend para gestión y priorización de videojuegos (Sprint 1)",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev": "lsof -ti:4000 | xargs -r kill -9 && node --watch src/server.js",
    "start": "node src/server.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev"
  },
  "dependencies": {
    "@prisma/client": "^6.1.0",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.3",
    "dotenv": "^16.4.1",
    "express": "^4.21.1",
    "express-rate-limit": "^8.5.2",
    "helmet": "^8.1.1",
    "jsonwebtoken": "^9.0.3",
    "morgan": "^1.10.1",
    "prisma": "^6.1.0",
    "zod": "^3.25.0"
  }
}
